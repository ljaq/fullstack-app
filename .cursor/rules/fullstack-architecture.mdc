---
description: 全栈项目结构、构建入口、路径别名与多页面前端
alwaysApply: true
---

# Fullstack 架构概览

## 技术栈

- **运行时**：Vite 8、`@hono/vite-dev-server` / `@hono/vite-build`，开发时入口为根目录 `app.ts`。
- **后端**：Hono 4、TypeORM、SQLite（`dev.db` / `prod.db`），环境变量来自 `.env.${mode}`。（仓库中另有 `Prisma` 依赖与 `server/prismaClient.ts`，与当前主业务入口 `getDataSource()` 并存，以代码为准。）
- **前端**：React 18、Ant Design、React Router 7、`@tanstack/react-query`。

## 目录约定

| 区域 | 路径 | 说明 |
|------|------|------|
| HTTP 入口 | `app.ts` | 挂载生成路由、多页 HTML、静态资源、`/api/*` 第三方代理 |
| 后端路由 | `server/routes/` | 文件即路由，见 `server-hono-routes` 规则 |
| 后端业务 | `server/services/`、`server/repositories/`、`server/container/` | **一律**按 [`server/ARCHITECTURE.md`](server/ARCHITECTURE.md)：路由只做 HTTP + `zValidator` + `getService()`；领域与数据访问不进 `server/routes/`。（Vite 仍排除 `**/*.service.ts` 以免误扫成路由，但勿在 routes 下新增业务 Service。） |
| 请求校验 | `server/routes/**/xxx.schema.ts` | 与路由同目录、同名 stem；Zod 对象（`zValidator` 入参）；`**/*.schema.ts` 同样被排除 |
| 开发快照 | `server/routes/**/xxx.snapshot.ts` | 与路由同目录、同名 stem；`defineDevSnapshot`（`server/dev-snapshot/define.ts`），产出 `*.snapshot.json`；`**/*.snapshot.ts` 排除于路由 |
| 前端多页 | `client/pages/<page>/` | 每页独立 `index.html` + `routes/`，懒加载路由表由插件生成 |
| 共享 API 层 | `api/` | `request` 代理 + `Fetch`；`api/app-type.ts` 从 `server/routes/_route.gen` 导出 `AppType`（避免 `import type` 时拉入整份 `app.ts`） |
| 类型 / 工具 | `types/`、`utils/` | 枚举与通用工具 |

## 路径解析与别名

- Vite 使用 `getSharedResolve()` 中的 `tsconfigPaths: true`（`vite/config/shared.ts`），与 `tsconfig` 的 `baseUrl` / `paths` 对齐，而非在 Vite 里写死一条别名表。
- 主应用见 `tsconfig.app.json` / `tsconfig.node.json`；`mini-program` 自有 `tsconfig` 与 `vite.config` 的 `alias`（含对根目录 `api`、`server` 等的映射）。

## 第三方接口

- `app.ts` 将 **`/api/*`** 代理到 `VITE_THIRD_API`（与本地 `/app` 业务接口分离）。
- `api/api.ts` 中 `authority` 等路径走 `/api/...`；自建后端走 `/app/...`（与 `vite-plugin-server-route` 的 `baseRoute` 一致）。

## 构建脚本

- `pnpm dev`：Vite development。
- `pnpm build`：`build:client`（`mode=client`）+ `build:server`（`mode=server`），产物在 `build/`。
