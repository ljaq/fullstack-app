---
description: Hono 文件路由、处理器导出与校验约定
globs: server/routes/**/*.ts
alwaysApply: false
---

# Server：Hono 文件路由

## 生成文件

- `server/routes/_route.gen.ts` **由 `vite-plugin-server-route` 自动生成，禁止手改**。
- 开发/构建时会扫描 `server/routes`，按文件路径映射为 URL（`basePath` 为 `/app`）。
- 动态段使用文件名如 `[id].ts`、目录 `[id]/index.ts`。

## 处理器导出

- 每个路由文件导出 **大写 HTTP 方法名**：`GET`、`POST`、`PUT`、`DELETE`。
- 使用 `createFactory()` + `factory.createHandlers(...)` 组合中间件与处理函数。

```ts
// 典型顺序：鉴权 → zValidator → handler
export const POST = factory.createHandlers(
  requireAuth,
  zValidator('json', bodySchema),
  async c => { /* ... */ },
)
```

## 校验与错误

- 使用 **`server/utils/zod-validator` 的 `zValidator`**，校验失败返回 `{ message, issues }` 结构，便于前端展示。
- 业务错误：`return c.json({ message: '...' }, 4xx)`。

## 数据与鉴权

- 数据库：`await getDataSource()`（`server/db.ts`），实体在 `server/entities/`。
- 鉴权：`requireAuth`、`getCurrentUser`、`signAuthToken` 等来自 `server/utils/auth.ts`；客户端用 `Authorization: Bearer <JWT>`，不设 Cookie。

## 开发快照（可选）

- `*.snapshot.ts`（与路由同名的快照配置，见 `vite-plugin-api-dev-snapshot`）被插件排除，不进入 `_route.gen.ts`；用于开发态请求快照。

## 同目录 Service

- 与路由 **同一路径**、**文件名** 为路由 stem + `.service.ts`（如 `index.ts` 与 `index.service.ts`、`[id].ts` 与 `[id].service.ts`）。
- **领域逻辑**（查询、事务、业务规则、DTO 映射）放在 `*.service.ts`；路由文件只做鉴权、`zValidator`、调用 service、`c.json` 与 HTTP 状态码。
- `*.service.ts` 在 [`vite/config/plugins.ts`](vite/config/plugins.ts) 中与 `*.snapshot.ts`（快照配置）、`*.schema.ts`（Zod 校验）一并 **exclude**，避免被扫成路由。

## 同目录 Schema（Zod）

- 与路由 **同一路径**、**文件名** 为路由 stem + `.schema.ts`，导出供 `zValidator` 使用的 Zod 对象（如 `paramSchema`、`createBody`）。
- `*.schema.ts` 与 `*.service.ts` 一样 **exclude**，不参与 URL 扫描。
